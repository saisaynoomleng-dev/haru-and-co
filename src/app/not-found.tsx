import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Not Found',
  description: `Even in luxury, paths can stray. Return to Haru&Co. to explore our latest designer collections and global arrivals.`,
};

const NotFoundPage = () => {
  return <div>NotFoundPage</div>;
};

export default NotFoundPage;
